Write a small Python program in this directory. Do not ask questions.

Files:
- fizzbuzz.py — function fizzbuzz(n) returns a string:
    multiples of 15 → "FizzBuzz"
    multiples of 3 → "Fizz"
    multiples of 5 → "Buzz"
    otherwise → str(n)
  Include `if __name__ == "__main__":` that prints fizzbuzz(i) for i in 1..20.
- test_fizzbuzz.py — unittest covering 1, 3, 5, 15, and 16.
- README.md — one short paragraph on how to run the tests.

Leave the files on disk when you are done.
