# Studio

This directory is the **product** workspace for the live host (`POST /think`).

The Living Dictionary writes whatever the GOAL asks for here — a web app, a
backend, a game, a Shen or Forth program, anything. Forth programs that
*build* the product, and the colon-word dictionary, live outside this tree.

There is no default task. Follow the GOAL.
